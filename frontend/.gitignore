# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# symlink to ../../data/sofia/web for dev — the repo root's `data/**` rule
# doesn't cover this path (it's anchored to the root), so it needs its own
# entry here. Never committed: it's a machine-local dev convenience, and prod
# reads from VITE_DATA_BASE_URL instead (see README.md).
public/data

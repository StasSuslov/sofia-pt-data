# Copy this file to .env.local (gitignored) and fill in your own values.
# .env.local is never committed — the VPS address specifically must not
# end up in a repo meant to be published.

VPS_HOST=root@203.0.113.5
# VPS_KEY=~/.ssh/sofia_pt_do
# REMOTE_DIR=/opt/sofia-pt/data/
